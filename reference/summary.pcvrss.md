# Summarize a `pcvrss` object.

Summarize a `pcvrss` object.

## Usage

``` r
# S3 method for class 'pcvrss'
summary(object, ...)
```

## Arguments

- object:

  An object of class `pcvrss` to method `summary` of `pcvrss`.

- ...:

  further arguments, passed to print.default.
